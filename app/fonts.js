import { Pattaya, Roboto_Mono } from "next/font/google";

export const pattaya = Pattaya({
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
});
